import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const WishList = () => {
  return (
    <View>
      <Text style={{fontSize:24,padding:10,fontWeight:'bold'}}>WishList</Text>
    </View>
  )
}

export default WishList

const styles = StyleSheet.create({})